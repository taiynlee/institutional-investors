[project]
name = "stock-main-force"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.3",
    "requests>=2.34.2",
    "scrapling[fetchers]>=0.4.8",
    "yfinance>=1.3.0",
]

[tool.uv.workspace]
members = [
    "backend",
]
